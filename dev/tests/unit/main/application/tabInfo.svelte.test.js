import { describe, it } from "vitest"
import {
  getLastFocusTabIdWritable,
  resetTabIdxToInfo,
  setLastFocusTabIdWritable,
  tabIdxToInfo,
} from "../../../../main/lib/application/tabInfo.svelte"

describe("tab idx to info", () => {
  it("", () => {
    // tabIdxToInfo
    // resetTabIdxToInfo
    // getLastFocusTabIdWritable
    // setLastFocusTabIdWritable
  })
})

describe("last focus tab id", () => {})
